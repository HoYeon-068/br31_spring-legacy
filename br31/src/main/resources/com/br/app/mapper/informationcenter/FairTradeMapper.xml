<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.br.app.mapper.informationcenter.FairTradeMapper">

  <!-- 목록 (ROWNUM 페이징) -->
  <select id="selectList" resultType="com.br.app.domain.informationcenter.FairTradeDTO">
    SELECT *
    FROM (
      SELECT ROWNUM rn, f.*
      FROM (
        SELECT
          NOTICE_ID,
          NOTICE_NO,
          TITLE,
          REG_DATE
        FROM FAIR_TRADE
        ORDER BY REG_DATE DESC
      ) f
    )
    WHERE rn BETWEEN #{startRow} AND #{endRow}
  </select>

  <!-- 전체 건수 -->
  <select id="getTotalCount" resultType="int">
    SELECT COUNT(*)
    FROM FAIR_TRADE
  </select>

  <!-- 상세 -->
  <select id="selectOne" resultType="com.br.app.domain.informationcenter.FairTradeDTO">
    SELECT
      NOTICE_ID,
      NOTICE_NO,
      TITLE,
      CONTENT,
      REG_DATE
    FROM FAIR_TRADE
    WHERE NOTICE_ID = #{noticeId}
  </select>

</mapper>
