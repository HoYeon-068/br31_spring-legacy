<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.br.app.mapper.informationcenter.PressMapper">

  <select id="selectList" resultType="com.br.app.domain.informationcenter.PressDTO">
    SELECT *
    FROM (
      SELECT ROWNUM rn, p.*
      FROM (
        SELECT
          PR_ID,
          PR_NO,
          TITLE,
          REG_DATE
        FROM BR31.PRESS_RELEASE
        ORDER BY REG_DATE DESC
      ) p
    )
    WHERE rn BETWEEN #{startRow} AND #{endRow}
  </select>

  <select id="getTotalCount" resultType="int">
    SELECT COUNT(*)
    FROM BR31.PRESS_RELEASE
  </select>

  <select id="selectOne" resultType="com.br.app.domain.informationcenter.PressDTO">
    SELECT
      PR_ID,
      PR_NO,
      TITLE,
      CONTENT,
      REG_DATE
    FROM BR31.PRESS_RELEASE
    WHERE PR_ID = #{id}
  </select>

</mapper>
