<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xmlns:beans="http://www.springframework.org/schema/beans"
   xmlns:context="http://www.springframework.org/schema/context"
   xsi:schemaLocation="
      http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
      http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
      http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

   <!-- 1) @Controller / @RequestMapping 활성화 -->
   <annotation-driven />

   <!-- 2) 정적 리소스 -->
   <resources mapping="/resources/**" location="/resources/" />
   <!-- 수업/프로젝트에서 폴더를 따로 쓰면 아래도 같이 -->
   <resources mapping="/css/**" location="/css/" />
   <resources mapping="/js/**" location="/js/" />
   <resources mapping="/images/**" location="/images/" />

   <!-- (선택) DefaultServlet로 정적 리소스 처리 보강 -->
   <default-servlet-handler />

   <!-- 3) 컴포넌트 스캔 (컨트롤러 포함) -->
   <context:component-scan base-package="com.br.app" />

   <!-- 4) Tiles 설정 (수업 구조) -->
   <!-- <beans:bean id="tilesConfigurer" class="org.springframework.web.servlet.view.tiles3.TilesConfigurer">
      <beans:property name="definitions">
         <beans:list>
            <beans:value>/WEB-INF/tiles/tiles.xml</beans:value>
         </beans:list>
      </beans:property>
   </beans:bean> -->

   <!-- Tiles ViewResolver (우선순위 1) -->
   <!-- <beans:bean id="tilesViewResolver" class="org.springframework.web.servlet.view.UrlBasedViewResolver">
      <beans:property name="viewClass" value="org.springframework.web.servlet.view.tiles3.TilesView" />
      <beans:property name="order" value="1" />
   </beans:bean> -->

   <!-- JSP ViewResolver (우선순위 2) -->
   <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
      <beans:property name="prefix" value="/WEB-INF/views/" />
      <beans:property name="suffix" value=".jsp" />
      <beans:property name="order" value="1" />
   </beans:bean>

   <!-- 5) 파일 업로드 (CommonsMultipartResolver) -->
   <beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
      <!-- -1: 제한 없음 (수업에서 자주 씀) -->
      <beans:property name="maxUploadSize" value="-1" />
      <beans:property name="maxInMemorySize" value="10240" />
      <beans:property name="defaultEncoding" value="UTF-8" />
   </beans:bean>

</beans:beans>
